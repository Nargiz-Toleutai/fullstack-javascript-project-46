name: main

on:
  push:
    branches:
      - '**'
    tags:
      - '**'

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: Hexlet project check
        uses: hexlet/project-action@release
        with:
          hexlet-id: ${{ secrets.MAINTAINABILITY_CODECLIMATE }}
          hexlet-id1: ${{ secrets.TEST_COVERAGE_CODECLIMATE }}
      - run: make setup
      - run: make test
      - run: make lint 
          
